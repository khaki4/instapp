// imports

// actions

// action creator

// api actions

// initial state
const initialState = {
  isLoggedIn: false,
}

// reducer

// reducer function
function reducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

// exports


// defuat reducer export

export default reducer;
